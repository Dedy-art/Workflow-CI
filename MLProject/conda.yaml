name: predictive_maintenance_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - mlflow
      - pandas
      - numpy
      - scikit-learn
      - dagshub
      - matplotlib
      - seaborn